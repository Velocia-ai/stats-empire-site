# Netlify continuous deployment config for the Stats Empire static export.
# The repo root IS the Next.js app, which builds to ./out via `output: 'export'`.
[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
